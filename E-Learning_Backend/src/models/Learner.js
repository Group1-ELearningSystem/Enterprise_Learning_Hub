class Learner {
    constructor(learnerId, fullName, emailAddress, accountNumber) {
        this.learnerId = learnerId
        this.fullName = fullName
        this.emailAddress = emailAddress
        this.accountNumber = accountNumber
    }
}

export default Learner