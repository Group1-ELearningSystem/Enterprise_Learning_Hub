class Account {
    constructor(accountNumber, username, password, role, status){
        this.accountNumber = accountNumber;
        this.username = username
        this.password = password
        this.role = role
        this.status = status
    }
}

export default Account;